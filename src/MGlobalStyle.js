import React, { Component } from 'react';
import { StyleSheet } from 'react-native';

module.export = StyleSheet.create({
   defaultFont: {
      fontFamily: 'San Franciso',
      fontWeight: 'bold',
   },
});
